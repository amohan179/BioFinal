services:
  - type: web
    name: bioproject
    env: python
    plan: free
    buildCommand: pip install -r backend/requirements.txt && rm -rf frontend/node_modules && npm --prefix frontend install --include=optional && npm --prefix frontend run build
    startCommand: uvicorn backend.main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: PYTHON_VERSION
        value: 3.12.7
      - key: NODE_VERSION
        value: 20.18.1
